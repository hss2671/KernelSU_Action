# KernelSU Action -- build profile
#
# Values here are defaults; workflow_dispatch inputs override them per run.
# Only the exact string "true" (or yes/on/1) counts as enabled.
#
# Copy this file to config/<device>.env to keep one profile per device, then
# pick it with the "Config file to build" input when you start a build.

CONFIG_ENV=config.env

# ============================================================ Kernel source ==

KERNEL_SOURCE=https://github.com/hss2671/mobvoi-ticwatch-kernel
KERNEL_SOURCE_BRANCH=mobvoi-android-msm-rover-4.9
KERNEL_CONFIG=rover_defconfig
KERNEL_IMAGE_NAME=zImage
ARCH=arm

# Optional: overrides the device label used in artifact names and sets
# CONFIG_LOCALVERSION. Leave blank to derive it from KERNEL_CONFIG.
KERNEL_NAME=

ADD_LOCALVERSION_TO_FILENAME=false

# Extra flags appended to every make invocation.
# Keep CLANG_TRIPLE here because this is a 32-bit ARM Android kernel.
# Do not set CROSS_COMPILE here: this 4.9 Makefile rejects the Android
# --target combination when CROSS_COMPILE is explicitly present.

EXTRA_CMDS=CC=clang CLANG_TRIPLE=arm-linux-androideabi-
CUSTOM_CMDS=

# =================================================================== Clang ==

CLANG_BRANCH=master-kernel-build-2022
CLANG_VERSION=r450784e

USE_CUSTOM_CLANG=false
CUSTOM_CLANG_SOURCE=
CUSTOM_CLANG_BRANCH=

# Build fully with LLVM (LLVM=1 LLVM_IAS=1).
# Keep disabled for this 4.9 kernel.
USE_LLVM=false

# ===================================================================== GCC ==

# ARM32 GCC is retained for the old ARMv7 kernel.
ENABLE_GCC_ARM64=false
ENABLE_GCC_ARM32=true

USE_CUSTOM_GCC_64=false
CUSTOM_GCC_64_SOURCE=
CUSTOM_GCC_64_BRANCH=
CUSTOM_GCC_64_BIN=aarh64-linux-android-

USE_CUSTOM_GCC_32=false
CUSTOM_GCC_32_SOURCE=
CUSTOM_GCC_32_BRANCH=
CUSTOM_GCC_32_BIN=arm-linux-androideabi-

# =============================================================== KernelSU ===

KSU_VARIANT=resukisu
KSU_REF=

# Old 4.9 non-GKI kernel: use manual hooks.
KSU_HOOK_MODE=manual

# Custom manager APK signature. Leave blank for the official manager.
KSU_EXPECTED_SIZE=
KSU_EXPECTED_HASH=

# =================================================================== SUSFS ==

ENABLE_SUSFS=false
SUSFS_REPO=https://gitlab.com/simonpunk/susfs4ksu.git
SUSFS_BRANCH=auto

# ========================================================== Extra patches ===

# Linux 4.9 needs the backported path_umount implementation.
ENABLE_PATH_UMOUNT=true

ENABLE_HIDE_STUFF=false

# KPM is arm64 only.
ENABLE_KPM=false

# ================================================================ Kconfig ===

ADD_KPROBES_CONFIG=false
ADD_OVERLAYFS_CONFIG=true
DISABLE_LTO=false
DISABLE_CC_WERROR=true

EXTRA_DEFCONFIG=

# ============================================================= AnyKernel3 ===

USE_CUSTOM_ANYKERNEL3=false
CUSTOM_ANYKERNEL3_SOURCE=https://github.com/osm0sis/AnyKernel3
CUSTOM_ANYKERNEL3_BRANCH=master

# ============================================================== Packaging ===

NEED_DTBO=false

# We will repack the boot image manually with magiskboot.
BUILD_BOOT_IMG=false
SOURCE_BOOT_IMAGE=https://raw.githubusercontent.com/hss2671/my-boot/main/boot.img

# ================================================================= Runner ===

ENABLE_CCACHE=true
REMOVE_UNUSED_PACKAGES=true
